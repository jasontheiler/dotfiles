set-option -ga terminal-features ",xterm-256color:RGB"
set-option -g mode-keys vi
set-option -g mouse on
set-option -g escape-time 10
set-option -g display-time 2000
set-option -g set-titles on
set-option -g set-titles-string "#{s|$HOME|~|:session_path}/"

set-option -g status-keys vi
set-option -g status-style bg="#0b0d0f"
set-option -g status-justify right
set-option -g status-left ""
set-option -g status-left-length 0
set-option -g status-right "#{?#{==:#{active_window_index},#{session_windows}},#[bg=white#,fg=red],#[bg=terminal#,fg=red]}#[bg=red,fg=yellow]#[bg=yellow,fg=green]#[bg=green,fg=#192d22]#[bg=#191d22,fg=terminal] #{s|$HOME|~|:session_path}/  Sessions: #(fish -c \"count (tmux list-sessions)\") "
set-option -g status-right-length 0

set-option -g base-index 1
set-option -g renumber-windows on
set-option -g window-status-style ""
set-option -g window-status-format "#{?window_start_flag,#[bg=#0b0d0f#,fg=#111316] ,#{?#{==:#{e|-:#{window_index},1},#{active_window_index}},#[bg=white#,fg=#111316] ,#[bg=terminal#,fg=terminal] }}#[bg=terminal,fg=terminal] #{window_index}: #{window_name} "
set-option -g window-status-current-format "#{?window_start_flag,#[bg=#0b0d0f#,fg=white],#[bg=terminal#,fg=white]} #[bg=white,fg=black] #{window_index}: #{window_name} "
set-option -g window-status-separator ""

set-option -g pane-base-index 1
set-option -g pane-border-lines heavy
set-option -g pane-border-style fg="#0b0d0f"
set-option -g pane-active-border-style fg="#0b0d0f"
set-option -g main-pane-width 70%

set-option -g popup-border-lines heavy
set-option -g popup-border-style fg="#222b30"

unbind-key C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind-key C-s choose-session -ZNF "#{session_path}"

bind-key s split-window -ve GREETING=false
bind-key v split-window -he GREETING=false

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key -r C-h resize-pane -L
bind-key -r C-j resize-pane -D
bind-key -r C-k resize-pane -U
bind-key -r C-l resize-pane -R

bind-key g display-popup -E -w 75% -h 80% "lazygit"
bind-key b display-popup -E -w 75% -h 80% "btm"

bind-key C-r source $HOME/.tmux.conf\; display "Config reloaded"
